name: curl
version: 8.10.1
url: https://curl.se/download/curl-8.10.1.tar.xz
steps:
  one:
    dependencies:
      - libidn2
      - libpsl
      - libssh2
      - perl
      - zlib
    run: build.bat
