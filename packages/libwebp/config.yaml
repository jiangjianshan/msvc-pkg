name: libwebp
version: 1.4.0
url: https://storage.googleapis.com/downloads.webmproject.org/releases/webp/libwebp-1.4.0.tar.gz
steps:
  one:
    dependencies:
      - zlib
      - libpng
      - libjpeg-turbo
      - tiff
      - giflib
      - winpthreads
    run: build.bat